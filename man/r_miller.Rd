% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/r_miller.R
\name{r_miller}
\alias{r_miller}
\title{Park–Miller Minimal Standard Generator (1988)}
\usage{
r_miller(n, seed)
}
\arguments{
\item{n}{Positive integer. Number of pseudorandom numbers to generate.}

\item{seed}{Initial integer in the range \eqn{[1, m-1]}. Generator seed.}
}
\value{
A list with two elements:
\describe{
  \item{\code{v_aleatorias}}{Numeric vector of length n with the generated uniform values in (0,1).}
  \item{\code{semilla_final}}{Integer corresponding to the last internal value \eqn{z_n} of the generator.}
}
}
\description{
Implementation of the Park and Miller (1988) multiplicative congruential
generator, also known as *Minimal Standard RNG*. Uses Schrage's method to
avoid overflow issues when computing a * seed.

This generator produces uniform pseudorandom numbers in (0,1) with a maximum
period of \eqn{m - 1 = 2,147,483,646}, where:

\itemize{
  \item \eqn{a = 16807}
  \item \eqn{m = 2^{31} - 1 = 2147483647}
  \item \eqn{q = 127773}
  \item \eqn{r = 2836}
}
}
\details{
Schrage's algorithm is implemented:

\deqn{
  z_{n+1} = a (z_n \bmod q) - r \left\lfloor \frac{z_n}{q} \right\rfloor
}

If the result is negative, the modulus \eqn{m} is added.

Uniform values are obtained through:

\deqn{
  u_n = \frac{z_n}{m}
}
}
\examples{
# Generate 5 numbers with seed 12345
r_miller(5, 12345)

# Classic validation from the article: z_10000 = 1043618065
r_miller(10000, 1)$semilla_final == 1043618065

# Generate a quick histogram
x <- r_miller(10000, 123)$v_aleatorias
hist(x, breaks = 20)

}
\references{
Park, S.K. & Miller, K.W. (1988). *Random Number Generators: Good Ones Are Hard to Find*.
}
